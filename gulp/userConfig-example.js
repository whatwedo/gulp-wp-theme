/**
 * User specific configuration for the build task
 * Copy and rename to userConfig.js to use your own settings
 * Will be merged with config.js.
 * Use require('./config.js') in your gulp files
 */
//var packageJson = require('../package.json');


// Public available for gulp resources
module.exports = {
  // Your local root url. Is used by browerSync as proxy
  localRootUrl: 'http://localhost:80'
};
